# Results — 2026-07-06

VM: Ubuntu 24.04 + LxQt, X11, compositing off, 4 vCPU / 8 GB (dev VM, warm —
see honesty notes in `../METHODOLOGY.md`). Commit: quick_tiles adaptive-cap
fix (this session), on top of `fd921b6`.

## Startup

App-internal first paint (`./benchmarks/measure.sh`, 5 consecutive runs,
1280x720, no other windows open):

| run | proc_s | rss_mb at first paint |
|-----|--------|------------------------|
| 1   | 0.194  | 135.5 |
| 2   | 0.182  | 135.2 |
| 3   | 0.180  | 135.1 |
| 4   | 0.195  | 135.2 |
| 5   | 0.187  | 135.1 |

p50 = 0.187 s, p95 = 0.195 s. Budget: < 3 s login-to-visible. **Met**, with
large headroom — and consistent with the last full packaged-install
measurement (login-to-visible = 0.621 s, G1 gate, 2026-06-12; not re-run this
session, see methodology).

## Idle CPU (30 s pidstat, dock + desktop widgets visible, no interaction)

| resolution | avg %CPU |
|------------|----------|
| 1280x720   | 0.10%    |
| 1920x1080  | 0.10%    |

Budget: < 10% (30 s avg). **Met**, ~100x under budget. The occasional 1%
single-second blips come from the auto-advancing carousel widget (~8 s
interval) and the clock's 1 s QTimer tick; both are the only permitted
polling per CLAUDE.md.

## Idle RSS

| resolution | VmRSS |
|------------|-------|
| 1280x720   | 145.5 MB |
| 1920x1080  | 155.7 MB |

Budget: < 200 MB. **Met**, ~55 MB headroom at 1280x720. (RSS is slightly
higher at 1920x1080 because more widget content — e.g. carousel images — is
visible at once and gets decoded/cached; still well inside budget.)

## 1280x720 rendering

Screenshots in `2026-07-06_screenshots/`, captured with compositing off via
a PyQt5 `grabWindow(0)` script run inside the VM (see METHODOLOGY.md):

- `desktop_1280x720_dark.png`, `desktop_1280x720_light.png` — full desktop,
  both themes.
- `menu_1280x720.png` — app menu overlay (categories + search + app grid).
- `widget_store_1280x720.png` — Widget Store overlay.
- `desktop_1920x945_crosscheck.png` — same desktop at the dev VM's native
  resolution, for comparison.
  